# Phase 1 Handoff: Core Schema Definitions

**Phase**: 1 — Core Schema Definitions [COMPLETED]
**Task**: 4 — Define training data schema and action space
**Session**: sess_1780078869_8696d2

## What Was Done

Created 5 Python modules in `src/bimodal_harness/schema/`:

- `constants.py`: SCHEMA_VERSION="1.0.0", VALID_FORMULA_TAGS (6), VALID_TOP_OPERATORS (8), VALID_FRAME_CLASSES (3), VALID_LABELS, VALID_DIFFICULTY_TIERS, VALID_SOURCES
- `formula.py`: AtomRepr dataclass, FormulaJson type alias, validate_formula_json(), formula_json_to_pretty() matching DataExport.lean
- `actions.py`: AXIOM_ACTIONS (42), RULE_ACTIONS (7), ALL_ACTIONS (49), ACTION_TO_INDEX, INDEX_TO_ACTION, BASE_MASK (44 True), DENSE_MASK (46 True), DISCRETE_MASK (47 True), FrameClass enum
- `records.py`: PatternKey, RuleProfile, ProofTrace, SimpleCountermodel, DifficultyMetrics, TrainingRecord dataclasses
- `schema/__init__.py`: Public API exports

## Verification

- `len(AXIOM_ACTIONS) == 42` ✓
- `len(ALL_ACTIONS) == 49` ✓
- BASE_MASK has 44 True values (37 axioms + 7 rules) ✓
- DENSE_MASK has 46 True values (39 axioms + 7 rules) ✓
- DISCRETE_MASK has 47 True values (40 axioms + 7 rules) ✓
- Action index mappings are bijective ✓

## Deviations from Plan

None.

## For Phase 2

Phase 2 adds validation.py and serialization.py.  These depend on all Phase 1 types.
