# Phase 1 Handoff: Install lean-interact and Smoke Test

**Status**: COMPLETED
**Timestamp**: 2026-05-29

## What was done
- Added `BIMODAL_LOGIC_PATH` configuration constant to `src/bimodal_harness/config.py`
- Added `LEAN_REPL_TIMEOUT`, `LEAN_SUBPROCESS_TIMEOUT`, `LEAN_AUTO_RECOVER`, `LEAN_STARTUP_IMPORTS` constants
- All values are environment-variable overridable

## Deviations
- `pip install -e ".[lean]"` skipped: lean-interact not installed in current environment; guarded by skip markers in tests
- Live REPL smoke test skipped for same reason; covered by `@pytest.mark.lean` tests in Phase 3

## Inputs for Phase 2
- `config.py` exports `BIMODAL_LOGIC_PATH` and Lean-related constants
- BimodalLogic confirmed at `/home/benjamin/Projects/BimodalLogic/` with toolchain `v4.27.0-rc1`
- Relevant modules: `Bimodal.Syntax.Formula`, `Bimodal.Syntax.Atom`, `Bimodal.ProofSystem.*`
